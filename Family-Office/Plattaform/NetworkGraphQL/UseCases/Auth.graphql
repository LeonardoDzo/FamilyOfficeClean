
mutation SignInUser($e:String!, $p:String!) {
    signInUser(emailProvider: {
        email: $e,
        password: $p
        }) {
            token,
            user {
                id,
                name,
                email
            }
    }
}
